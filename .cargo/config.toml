[alias]
lint-clippy = "clippy --workspace --all-targets -- -D warnings"
fix-clippy = "clippy --workspace --all-targets --fix --allow-dirty --allow-staged"
lint-fmt = "fmt --all -- --check"
fix-fmt = "fmt --all"
